# TODO - update these values
name: samsshack
description: project description
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

# ==================================================
# DEPENDENCIES =====================================
# ==================================================
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # internet
  http: 0.13.4
  uni_links: 0.5.1
  connectivity_plus: 2.2.0
  url_launcher: 6.0.12
  # utility
  intl: 0.17.0
  package_info_plus: 1.3.0
  # local storage
  flutter_dotenv: 5.0.2
  shared_preferences: 2.0.11
  # navigation
  routemaster: 0.9.5
  # ui
  fluttertoast: 8.0.6

# ==================================================
# DEV DEPENDENCIES =================================
# ==================================================
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: 1.2.4
  flutter_lints: 1.0.4
#  test: ^1.0.0
#  mockito: 4.1.4
#  flutter_launcher_icons: ^0.9.0
#  flutter_test:
#    sdk: flutter


# TODO - set mobile app icons
# https://pub.dev/packages/flutter_icons
#flutter_icons:
#  android: "launcher_icon"
#  ios: true
#  image_path: "assets/icon/icon.png"
#  adaptive_icon_background: "assets/icon/background.png"
#  adaptive_icon_foreground: "assets/icon/foreground.png"

# TODO - set splash color
# This is a native splash for primarily Android and iOS.
# There is still an explicit splash screen widget
# that shows up once Flutter 'takes over'. This
# is just a page in the /pages dir that you will
# probably also need to edit.
# https://pub.dev/packages/flutter_native_splash
flutter_native_splash:
  color: "#42a5f5"
  #image: assets/splash.png


# ==================================================
# FLUTTER ==========================================
# ==================================================
flutter:
  uses-material-design: true
  assets:
    - assets/env/
    - assets/images/
    - assets/
  fonts:
    - family: Pacifico
      fonts:
        - asset: assets/fonts/Pacifico.ttf